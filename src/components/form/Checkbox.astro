---
// see: https://x.com/tak_dcxi/status/1801816383568634176?s=12
---

<label>
  <input type="checkbox" />
  <p>チェックボックス</p>
</label>

<style>
  label {
    display: grid;
    grid-template-columns: max-content 1fr;
    gap: 0.5rem;
  }

  [type='checkbox'] {
    display: grid;
    place-content: center center;
    width: 20px;
    height: 20px;
    appearance: none; /* input要素のデフォルトスタイルを無効化 */
    border: 1px solid #333;
    border-radius: 4px;

    &:checked {
      /* チェックボックスがアクティブ時のスタイル */
      background-color: #7af2ff5a;

      &::before {
        /* チェックマークはナナメにしたボックスの2辺をborderにすることで再現 */
        width: 5px;
        height: 11px;
        margin-bottom: 3px; /* チェックマークの位置を調整 */
        content: '';
        border-right: 3px solid #000;
        border-bottom: 2px solid #000;
        transform: rotate(41deg);
      }
    }
  }
</style>
