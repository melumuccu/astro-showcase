---
import Layout from './_Layout.astro'

const title = 'Astro showcase'
const subtitle = ''
const description =
  ''
---

<Layout title={`${title} ${subtitle}`} description={description} path={'/'}>
</Layout>
